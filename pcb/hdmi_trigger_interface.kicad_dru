(version 1)
(rule "class 1:0"
  (condition "A.NetClass == 'diff' && B.NetClass == 'Default'")
  (constraint clearance (min 0.2032mm)))

(rule "class 1:1"
  (condition "A.NetClass == 'diff' && B.NetClass == 'diff'")
  (constraint clearance (min 0.2032mm)))
